/**
 * The Map component contains 
 * div container with the "map"
 * id for rendering Yandex.Maps
 */
import React from 'react';

export default function Map() {
    return (
        <div id="map"></div>
    );
}
